# Migration Environment Configuration (for local development)

# Database Configuration (use localhost instead of postgres container name)
DB_DRIVER=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres123
DB_NAME=microservice_db
DB_SSLMODE=disable

# Database Connection Pool Configuration
DB_MAX_OPEN_CONNECTIONS=25
DB_MAX_IDLE_CONNECTIONS=10
DB_CONNECTION_MAX_LIFETIME=5m

# Logger Configuration
LOG_LEVEL=info
LOG_FORMAT=text
